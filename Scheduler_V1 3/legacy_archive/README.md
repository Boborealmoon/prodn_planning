Legacy Archive

This folder keeps historical copies of retired scheduler assets.

The active Gantt UI now lives in `scheduler_app/templates/gantt.html`.
The old root `templates/gantt.html` copy was removed because it used legacy `/api/rows` behavior and is no longer part of the app.
